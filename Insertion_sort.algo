PROCEDURE insertionSort(n:INTEGER, A : ARRAY_OF INTEGER)
VAR
    n,A,i,j : INTEGER;
BEGIN
    n:=length(A);
    FOR i FROM 0 TO n-1 DO
        j=i;
        WHILE (j > 0 and A[j-1] > A[j]) DO
            swap(A[J], A[j-1]);
            j=j-1;
        END_WHILE
    END_FOR
END
